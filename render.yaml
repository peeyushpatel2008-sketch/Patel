services:
  - type: web
    name: patel-pos-v24
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT pos.app:app
    healthCheckPath: /
    autoDeploy: true
